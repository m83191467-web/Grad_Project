# Legacy screens

The application entry point and active navigation use `lib/presentation/` and
`lib/features/`. The Dart files in this directory are retained temporarily for
reference and should not receive new features. New screens belong in the active
feature-based structure.
